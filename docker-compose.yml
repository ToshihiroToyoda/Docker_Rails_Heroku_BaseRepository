version: '3'
services:
  db:
    image: postgres:11.1
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/test_app_for_heroku_with_docker-compose
    ports:
      - "3000:3000"
    depends_on:
      - db
